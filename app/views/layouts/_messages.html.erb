<% flash.each do |key, value| %>

<div id="flash" data-notify="container" class="col-xs-11 col-sm-4 alert alert-<%= flash_class_name(key) %> alert-with-icon animated fadeInDown" role="alert" data-notify-position="top-right" style="display: inline-block; margin: 0px auto; position: fixed; transition: all 0.5s ease-in-out; z-index: 1031; top: 20px; right: 20px;">

  <button type="button" aria-hidden="true" class="close" data-dismiss="alert" style="position: absolute; right: 10px; top: 50%; margin-top: -13px; z-index: 1033;">×</button>

  <span data-notify="message"><%= value %></span>
</div>

<% end %>
